<h1>Web Apps</h1>

<%= link_to "Create new item", new_portfolio_path %>

<% @portfolio_items.each do |item| %>
  <h2><%= link_to item.title, portfolio_show_path(item) %></h2>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>s
  <p><%= image_tag item.thumb_image unless item.thumb_image.nil? %></p>

  <%= link_to "Edit", edit_portfolio_path(item ) %>
  <%= link_to "Remove item", item, method: :delete, data: { confirm: "Are you certain?" } %>
<% end %>