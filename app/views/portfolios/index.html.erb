<h1>My Portfolio</h1>

<% @portfolio_items.each do |item| %>
  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>s
  <p><%= image_tag item.thumb_image if !item.thumb_image.nil? %></p>
<% end %>